# CP3 metrics schema

`output/comparison.md` must include at least:

| Backend | ATE RMSE | RPE |
|---------|----------|-----|
| KISS-ICP | … | … |
| FAST-LIO2 | … | … |

Also record:

- evo alignment flags used (`-a`, etc.)
- GT source (RTK / NTRIP) and outage notes
- Path to cleaned PCD and Autoware map load command

Optional third backend (LIO-SAM): add a row; not required to pass.
