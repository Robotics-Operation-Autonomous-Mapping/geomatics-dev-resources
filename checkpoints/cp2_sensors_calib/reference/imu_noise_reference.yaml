# Placeholder reference — not KITTI ground truth.
# KITTI OXTS is a moving vehicle, so Allan numbers will not look like a static IMU
# datasheet. Oral: explain why. Replace this file if the team publishes a static log.
accelerometer:
  noise_density: 0.0
  random_walk: 0.0
  units:
    noise_density: m/s^2/sqrt(Hz)
    random_walk: m/s^3/sqrt(Hz)
gyroscope:
  noise_density: 0.0
  random_walk: 0.0
  units:
    noise_density: rad/s/sqrt(Hz)
    random_walk: rad/s^2/sqrt(Hz)
